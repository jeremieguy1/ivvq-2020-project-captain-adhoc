dist: trusty
language: java
jdk: oraclejdk8
services:
  - docker
script:
  - docker build -t "captain-adhoc:latest" .
deploy:
    provider: script
    script: bash docker_push
    on:
      all_branches: true
